

<%= form_with(model: @task, local: true) do |f| %>
    <p>
        <%= f.label :title %><br/>
        <%= f.text_field :title %> 
    </p>

    <p>
        <%= f.label :status %><br/>
        <%= f.check_box :status %>
    </p>

    <p>
        <%= f.submit %>
    </p>

<% end %>